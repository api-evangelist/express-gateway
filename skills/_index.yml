generated: '2026-09-07'
method: generated
source: >-
  openapi/_original/express-gateway-openapi.yml (every operationId verified
  present in the spec) + https://www.express-gateway.io/docs/admin/
provider: Express Gateway
providerId: express-gateway
description: >-
  Packaged Agent Skills for the Express Gateway Admin API, written against the two
  flows the API actually supports end to end: onboarding a consumer, and managing
  the scopes that gate them. Both are grounded in real operationIds and carry the
  conventions an agent needs — no idempotency, reversible deactivate versus
  irreversible delete, undocumented errors, and the self-hosted localhost base.
note: >-
  These skills are generated by API Evangelist. Express Gateway publishes no
  skills, no AGENTS.md and no llms.txt of its own, and the OpenAPI they are
  grounded in was written from the published Admin API Reference rather than
  harvested from the provider.
skills:
- name: express-gateway-onboard-api-consumer
  file: express-gateway-onboard-api-consumer.md
  api: express-gateway:express-gateway-users-api
  description: Create a user, register an app, issue a credential, and grant its scopes.
  operations:
  - createUser
  - createApp
  - createCredential
  - setCredentialScopes
  - listConsumerCredentials
  - getUser
- name: express-gateway-manage-scopes
  file: express-gateway-manage-scopes.md
  api: express-gateway:express-gateway-scopes-api
  description: Declare, inspect, grant and revoke the permission tags that gate consumer credentials.
  operations:
  - listScopes
  - getScope
  - createScope
  - createScopes
  - deleteScope
  - setCredentialScopes
  - addCredentialScope
  - removeCredentialScope
  - listConsumerCredentials
maintainers:
- FN: Kin Lane
  email: kin@apievangelist.com
